name: Sync version + tag

# chart.html is gitignored on main and force-added only on the tag commit.
# CocoaPods and SPM consumers resolve by git tag, so they still see chart.html
# at the tagged ref; main stays clean to avoid merge conflicts on feature branches.

on:
  repository_dispatch:
    types: [acttrader-charts-release]

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          # Check out with the PAT so the tag push below carries a non-Actions
          # identity. GitHub deliberately does not trigger workflows for refs
          # pushed with the default GITHUB_TOKEN, so publish.yml (`on: push:
          # tags`) never fired for a tag this job created — no tests ran and no
          # GitHub Release was cut for v1.3.0-beta.11, .12 or .14. The PAT was
          # already a secret here, but only reached the `gh` CLI via GH_TOKEN,
          # never `git push`.
          token: ${{ secrets.CHARTS_REPO_PAT }}

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump podspec version on main
        run: |
          sed -i "s/s\.version\s*=\s*'[^']*'/s.version          = '${{ github.event.client_payload.version }}'/" ActtraderCharts.podspec
          git add ActtraderCharts.podspec
          git diff --cached --quiet && echo "Nothing to commit, skipping." && exit 0
          git commit -m "chore: bump version to v${{ github.event.client_payload.version }}"
          git push origin HEAD:main

      - name: Build release commit with bundled chart.html
        env:
          GH_TOKEN: ${{ secrets.CHARTS_REPO_PAT }}
        run: |
          mkdir -p Sources/ActtraderCharts/Resources
          gh release download "v${{ github.event.client_payload.version }}" \
            --repo acttrader/ActCharts \
            --pattern "chart.html" \
            --output Sources/ActtraderCharts/Resources/chart.html \
            --clobber
          git add -f Sources/ActtraderCharts/Resources/chart.html
          # --allow-empty: chart.html is meant to be gitignored on main, but if a
          # copy ever lands there and matches the release asset byte for byte,
          # `git commit` exits 1 with "nothing to commit" and the release dies
          # before `git tag` — leaving npm and Android published while iOS has no
          # tag for SPM/CocoaPods to resolve (v1.3.0-beta.13, run 34212403766).
          # The tag commit must be created either way.
          git commit --allow-empty -m "release: bundle chart.html for v${{ github.event.client_payload.version }}"
          git tag "v${{ github.event.client_payload.version }}"
          git push origin "v${{ github.event.client_payload.version }}"
